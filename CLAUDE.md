# JSON 데이터 파싱 라이브러리 PoC

## 프로젝트 목적

다양한 JSON 파싱 라이브러리의 성능, 사용성, 안정성을 비교·검증하기 위한 Proof of Concept 프로젝트입니다.

실제 서비스 도입 전 라이브러리의 적합성을 평가하고, 요구사항에 맞는 최적의 파싱 전략을 선정하는 것이 목표입니다.

## 검증 범위

- 기본 JSON 파싱 및 직렬화/역직렬화
- 중첩 구조, 배열, 대용량 JSON 처리
- 스키마 검증 및 타입 안전성
- 파싱 성능 벤치마크 (처리 속도, 메모리 사용량)
- 에러 핸들링 및 예외 케이스 대응

## JSON 저장 라이브러리 PoC

Python 표준 라이브러리 및 서드파티 라이브러리를 활용하여 데이터를 JSON 형식으로 저장하는 방법을 검증합니다.

### 검증 라이브러리

| 라이브러리 | 설명 |
|---|---|
| `json` (표준) | 기본 직렬화·파일 저장 |
| `pydantic` | 모델 기반 직렬화 후 저장 |
| `orjson` | 고성능 JSON 직렬화·저장 |

### 검증 항목

- 객체/리스트를 JSON 파일로 저장 (`json.dump`, `orjson.dumps`)
- pydantic 모델을 JSON으로 직렬화 후 파일 저장 (`model_dump_json`)
- 저장 시 인코딩(UTF-8), 들여쓰기, 정렬 옵션 처리
- 대용량 데이터 저장 성능 비교 (`json` vs `orjson`)

### 벤치마크 결과

**파싱 (역직렬화)** — 사용자 2건 JSON 데이터, 10000회 반복

| 라이브러리 | 소요 시간 | 상대 성능 |
|---|---|---|
| `json` (표준) | 0.0436s | 기준 |
| `orjson` | 0.0153s | **2.8x 빠름** |
| `pydantic` | 0.0983s | **2.3x 느림** (스키마 검증 비용) |

**저장 (직렬화)** — 사용자 2건 JSON 데이터, 1000회 반복

| 라이브러리 | 소요 시간 | 상대 성능 |
|---|---|---|
| `json` (표준) | 0.0055s | 기준 |
| `orjson` | 0.0008s | **6.6x 빠름** |

## 디렉토리 구조

```
jsonDataParsing/
├── CLAUDE.md          # 프로젝트 문서
├── src/               # 파싱 구현 코드
├── data/              # 테스트용 JSON 샘플 파일
└── tests/             # 단위 테스트 및 벤치마크
```

## 개발 환경

- 플랫폼: Windows 11
- 작업 디렉토리: `C:\reviewer\PoC\jsonDataParsing`
